%article
  %h1
    %a{:href=>"#{post.slug}"}=post.title
  %cite{:class=>'l'} Published by #{post.account.name unless post.account.nil?} <time datetime="#{post.created_at.strftime('%Y-%m-%d')}">#{post.created_at.strftime('%A, %d.%m.%Y')}</time>
  .postbody
    =post.body
